# AIDraw 项目优化建议

> 评审日期：2026-09-04<br>
> 当前版本：v1.4.10<br>
> 当前提交：`8ec01c3 release: v1.4.10 security recovery, credential lifecycle, and canvas motion optimization`

## 1. 评审结论

当前项目的基础状态健康，不需要进行推倒式重构。类型检查、单元测试和生产构建均能通过，弹窗已经进行懒加载，样式也已从超大单文件拆分为领域模块。

现阶段最值得优先处理的不是继续调整视觉或机械拆分文件，而是以下三类问题：

1. 错误恢复和备份中的安全闭环。
2. 大量图片盒子场景下的无效重渲染和媒体内存占用。
3. API 类型检查、核心交互测试和组件职责边界。

建议先用一个小版本完成恢复、备份、密钥管理和类型检查，再单独开展画布性能优化。

## 2. 当前质量基线

本次执行结果：

| 检查项 | 结果 |
| --- | --- |
| `npm run check` | 通过 |
| `npm test` | 21 个测试文件、218 项测试全部通过 |
| `npm run build` | 通过 |
| 主入口 JavaScript | 295.10 kB，gzip 93.01 kB |
| 主样式文件 | 207.90 kB，gzip 34.26 kB |
| Git 工作区 | 检查时无未提交改动 |

当前已经做得比较好的部分：

- Provider 请求、鉴权和状态处理具有明确的模块边界。
- IndexedDB、Web Locks、BroadcastChannel 和队列机制能够支撑纯前端工作流。
- 远程媒体地址具备协议、重定向和内网地址校验。
- `media-proxy` 已经使用流式管道转发媒体。
- 个性化设置具备版本化存储、迁移、约束和单元测试。
- 图片盒子标题已经使用原生按钮实现点击复制，同时支持键盘操作。
- 大型弹窗已经通过 `React.lazy` 拆分为独立 JavaScript chunk。

## 3. 优先级总览

| 优先级 | 优化项 | 主要收益 | 预计范围 |
| --- | --- | --- | --- |
| P0 | 修复界面偏好重置键名 | 保证崩溃后能够真正恢复 | 小 |
| P0 | 紧急备份过滤敏感凭据 | 避免备份文件泄露 API Key | 小 |
| P1 | 补全 API Key 删除能力 | 完成本地凭据生命周期管理 | 中 |
| P1 | 扩大 TypeScript 检查范围 | 提前发现 API 和构建配置错误 | 小到中 |
| P1 | 减少画布定时无效重渲染 | 提升大量盒子时的流畅度 | 中 |
| P1 | 降低媒体转存内存峰值 | 减少视频转存失败或函数崩溃 | 中 |
| P1 | 增加核心 UI 和端到端测试 | 降低发布后的交互回归 | 中 |
| P2 | 按职责拆分大型组件 | 提高可维护性和可测试性 | 中到大 |
| P2 | 优化大画布可见区域渲染 | 支撑数百个历史盒子 | 大 |
| P2 | 按功能延迟加载样式 | 进一步缩小首屏资源 | 中 |
| P3 | 部署侧全局限流 | 提高公开部署的抗滥用能力 | 取决于部署环境 |

## 4. 详细优化建议

### 4.1 P0：修复“重置界面偏好”失效

#### 现状

`client/src/components/ui/ErrorBoundary.tsx` 的 `handleResetPreferences` 删除：

- `aidraw-ui-preferences`
- `aidraw-page-preferences`

当前个性化设置实际使用的键名则是：

```text
aidraw-ui-preferences-v1
```

它定义在 `client/src/lib/uiPreferences.ts` 的 `UI_PREFERENCES_STORAGE_KEY` 中。

#### 影响

如果当前版本的界面偏好导致渲染异常，用户点击“重置界面偏好并刷新”后，损坏数据仍然存在，页面可能再次进入相同的错误界面。

#### 建议

- 在 `ErrorBoundary` 中直接导入并使用 `UI_PREFERENCES_STORAGE_KEY`，避免再次出现字符串漂移。
- 当前键和确认需要兼容的旧键可以一起删除。
- 增加点击按钮后的测试，断言当前键被删除并触发刷新。

#### 验收标准

- 写入损坏的 `aidraw-ui-preferences-v1` 后，点击重置按钮能够删除该键。
- 页面刷新后使用安全默认配置启动。
- 主题和桌宠旧设置的处理符合当前迁移策略。

### 4.2 P0：紧急备份需要默认过滤 API Key

#### 现状

`client/src/components/ui/ErrorBoundary.tsx` 的紧急备份会读取整个 `settings` 对象仓库，并将 `settingsEntries` 原样写入下载的 JSON。

`client/src/lib/storage/settings.ts` 中的设置对象包含：

- `apiKey`
- `savedApiKeys`
- `savedApiKeyProviderIds`

#### 影响

备份不会自动上传网络，但文件中包含明文凭据。用户将备份发送给他人排查问题、上传网盘或提交到 Issue 时，可能意外泄露 API Key。

#### 建议

- 紧急备份默认不导出 `apiKey` 和 `savedApiKeys`。
- 可以保留非敏感设置，例如 Provider、Base URL 和模型。
- 如果未来确实需要备份凭据，应设计成单独选项，并在导出前明确提示风险。
- 增加测试，断言备份 JSON 中不存在明文密钥。

#### 验收标准

- 默认紧急备份中无法搜索到任何已保存 API Key。
- 文件夹、任务、附件和非敏感设置仍可正常导出。
- 错误界面明确说明备份是否包含凭据。

### 4.3 P1：补完整的 API Key 删除能力

#### 现状

`UpdateImageProviderSettingsPayload.clearApiKey` 的注释是“清除已保存的 API Key”，但 `providerSettingsApi` 仅执行：

```text
settings.apiKey = ""
```

历史 `savedApiKeys` 仍保存在 IndexedDB 中。同时，当前设置界面只支持导入和切换，没有删除某个历史 Key 的入口。

#### 影响

- 用户无法从界面真正移除失效或不再使用的密钥。
- `clearApiKey` 的类型说明和实际行为不一致。
- 备份或本地设备被读取时，旧密钥仍然存在。

#### 建议

- 将“取消当前激活”和“删除已保存凭据”拆成两个明确动作。
- 新增按索引或稳定 ID 删除凭据的 API。
- 删除当前激活项后，明确选择下一个可用 Provider Key，或者进入未配置状态。
- 删除被运行中任务绑定的 Key 前给出提示，因为任务恢复可能依赖它。
- 为多 Provider、DeepSeek Key、当前激活项和最后一个 Key 增加边界测试。

### 4.4 P1：扩大 TypeScript 严格检查范围

#### 现状

`client/tsconfig.json` 当前只有：

```json
"include": ["src"]
```

因此 `npm run check` 没有覆盖：

- `client/api/**/*.ts`
- `client/vite.config.ts`

#### 影响

媒体代理、请求守卫、DeepSeek 转发和 Vite 开发中间件中的类型错误，可能无法被日常检查提前发现。

#### 建议

- 保留浏览器代码的 `tsconfig.json`。
- 新增 Node 环境使用的 `tsconfig.node.json` 或 `tsconfig.api.json`。
- 将 `client/api` 和 `vite.config.ts` 纳入 Node 配置。
- 根目录的 `npm run check` 同时运行两套检查。

### 4.5 P1：减少图片盒子的定时无效重渲染

#### 现状

`App.tsx` 每 10 秒调用一次 `loadQueue`，并直接写入新的 `queue` 和 `providerSettings` 对象。即使数据没有变化，也会触发 `App` 更新。

与此同时，传给 `WorkflowCanvas` 的移动、删除、重绘、上传等操作函数多数会在每次 `App` 渲染时重新创建。`WorkflowCanvas` 会遍历全部任务渲染 `JobCard`，这些新函数引用会使 `JobCard` 外层的 `memo` 难以生效。

#### 影响

任务盒子较多时，即使画布内容没有改变，也可能随着队列轮询周期重复渲染全部盒子。

#### 建议

- 对 `queue` 和 `providerSettings` 增加快照相等判断，相同数据不更新状态。
- 稳定所有传给每个 `JobCard` 的操作回调。
- 将队列统计、公告和弹窗状态与画布渲染树隔离。
- 合并短时间内由状态事件、可见性变化和轮询同时触发的刷新。
- 使用 React Profiler 对比优化前后的提交次数和渲染耗时。

#### 验收标准

- 队列数据未变化时，10 秒轮询不会导致全部 `JobCard` 重新渲染。
- 单个任务状态变化时，只更新受影响的盒子和必要的连接线。
- 拖拽、搜索定位和个性化布局行为保持不变。

### 4.6 P1：降低媒体转存的内存峰值

#### 现状

`client/api/_mediaTransfer.ts` 会把远程媒体的所有数据块保存在 `chunks` 数组中，最大允许 200MB，随后再创建 `Blob` 和 `FormData` 上传。

#### 影响

接近上限的视频可能同时存在于数据块、Blob 和上传编码过程中，实际峰值内存可能明显超过 200MB。在内存受限的函数环境中容易失败。

#### 建议

- 优先研究远程响应到图床请求的真正流式转发。
- 如果目标图床不支持流式 multipart，短期将上限降低到符合部署内存的安全值。
- 对图片和视频设置不同上限。
- 在读取前检查 `Content-Length`，读取过程中继续保留累计字节保护。
- 增加接近上限、无 `Content-Length`、超时和中途断流测试。

### 4.7 P1：增加核心 UI 和端到端测试

#### 现状

当前测试总量不错，但 React 组件测试只有 `ErrorBoundary.test.tsx`。个性化设置主要覆盖配置工具和 Hook，没有覆盖真实抽屉、图片盒子和完整工作流。

#### 建议优先覆盖

1. 图片盒子标题点击复制、键盘触发和失败反馈。
2. 工具按钮显隐、排序和任务能力过滤。
3. 附件列表、堆叠、首图加数量和临时展开。
4. 个性化设置的预览、保存、取消和刷新恢复。
5. 错误边界的偏好重置和备份脱敏。
6. 创建任务、进入队列、生成完成、重绘和删除的完整浏览器流程。
7. 720px、1120px 和宽屏三档响应式安全布局。

### 4.8 P2：按职责拆分大型组件

当前主要大型文件：

- `client/src/components/panels/CreateJobPanel.tsx`
- `client/src/App.tsx`
- `client/src/components/panels/PersonalizationDrawer.tsx`
- `client/src/components/canvas/JobCard.tsx`

建议按职责而不是按行数拆分：

- `App`：提取工作区数据、任务操作、队列健康状态和弹窗状态。
- `CreateJobPanel`：拆分基础参数、模型能力参数、参考图和草稿持久化。
- `JobCard`：拆分动作轨道、附件轨道、历史版本轨道和媒体主体。
- `PersonalizationDrawer`：拆分预设、页面、图片盒子和外观四个设置区。

拆分完成后，各领域逻辑应拥有独立测试，避免只把 JSX 移到新文件而仍然共享一个巨型状态对象。

### 4.9 P2：为大画布增加可见区域裁剪

#### 现状

`WorkflowCanvas` 会渲染全部 `positionedJobs` 和连接线。图片已经使用 `loading="lazy"` 和 `decoding="async"`，但不可见任务仍然会创建 React 组件和 DOM。

#### 建议

- 根据当前平移、缩放和实际可视区域计算 `visibleCanvasRect`。
- 只渲染可视区域加一定 overscan 范围内的重型盒子内容。
- 拖拽目标、搜索目标和最新结果定位目标需要暂时强制渲染。
- 非可见盒子可以保留轻量占位或连接线元数据。

该项只有在单文件夹经常出现数百个任务时才需要优先实施。

### 4.10 P2：进一步优化首屏样式资源

#### 现状

弹窗 JavaScript 已经懒加载，但 `client/src/styles.css` 会在入口一次性导入全部样式模块，包括：

- `modals.css`
- `personalization.css`
- `error-boundary.css`
- `duomi.css`

#### 建议

- 将只服务于懒加载功能的样式跟随对应组件加载。
- 检查拆分前遗留的重复选择器和已失效样式。
- 保留 `tokens`、基础布局、画布和创作框等首屏必需样式。
- 优化后对比首屏 CSS、首次内容绘制和弹窗首次打开时间。

当前 CSS 的 gzip 体积为 34.26 kB，因此这一项不应早于安全、类型和渲染问题。

### 4.11 P3：部署侧增加全局限流

`client/api/_requestGuard.ts` 已明确实现为单实例内存限流。多实例或冷启动环境中，各实例不会共享计数。

如果接口公开部署，建议在部署平台配置全局限流或 WAF；代码内限流继续作为本地和单实例的第一层保护。不要为了这一项改变当前纯前端 Provider 调用架构。

## 5. 推荐实施顺序

### 第一阶段：安全与恢复闭环

建议作为一个小版本完成：

1. 修复个性化设置重置键名。
2. 紧急备份过滤 API Key。
3. 增加已保存 API Key 删除能力。
4. 增加对应单元测试和组件测试。
5. 更新版本公告，说明备份安全和密钥管理变化。

### 第二阶段：工程质量与画布性能

1. 将 `client/api` 和 Vite 配置纳入 TypeScript 检查。
2. 为队列状态增加相等判断。
3. 稳定图片盒子操作回调。
4. 添加核心工作流浏览器测试。
5. 使用 React Profiler 记录优化前后结果。

### 第三阶段：规模化能力

1. 按职责拆分大型组件。
2. 为数百任务场景增加可见区域裁剪。
3. 优化媒体流式转存。
4. 按功能延迟加载非首屏 CSS。
5. 根据实际部署环境增加全局限流。

## 6. 每个阶段的统一验证

完成修改后至少执行：

```powershell
# 严格类型检查
npm run check

# 全量单元测试
npm test

# 生产构建和分包检查
npm run build

# 检查补丁中的空白和换行问题
git diff --check
```

涉及画布或个性化布局时，还应在以下宽度进行浏览器验证：

- 大于 1120px 的完整桌面布局。
- 721px 到 1120px 的安全停靠布局。
- 小于等于 720px 的移动端安全布局。

## 7. 最终建议

当前项目最合理的方向是“小步补安全闭环，再用数据驱动画布性能优化”。不建议仅因为文件较大就马上全面重构，也不建议改变现有纯前端 Provider 架构。

第一阶段完成后，项目在崩溃恢复、备份分享和本地密钥管理方面会明显更可靠；第二阶段则能直接改善任务数量增加后的画布体验。
